## Description

The script takes one argument, the filename, and creates a copy of it with a timestamp.

Example:

```bash
bash script_mk_bk.sh dump.txt
```

The result is:

```bash
dump.txt_backup_20251125_081551
```

